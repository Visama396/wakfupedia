---
import Layout from '@/layouts/Layout.astro'
import items from '@items'
import { insertEffect } from '../utils/insertItem'

let helmet1 = items.find(item => item.definition.item.baseParameters.itemTypeId == 134)
let effects = helmet1.definition.equipEffects

const effect = effects[0]

console.log(effect.id)

const response = await insertEffect(effect.id, effect.actionId, effect.params)

console.log(response)
---
<Layout title="Testing">

</Layout>