---
import Layout from '@/layouts/Layout.astro'
import items from '@items'
import { insertItem } from '../utils/insertItem'

for (const item of items) {
    if (item.definition.item.baseParameters.itemTypeId == 134) {
        const response = await insertItem(item.definition.item.baseParameters.itemTypeId, item)
        console.log(response)
    }
}

/**
 * {
    "name": "armas",
    "ids": [101, 108, 110, 111, 112, 113, 114, 115, 117, 189, 223, 253, 254]
  }, 
  {
    "name": "armaduras",
    "ids": [103, 119, 120, 132, 133, 134, 136, 138]
  }, 
 */

---
<Layout title="Testing">
    <h2>Testing Appwrite</h2>
</Layout>