---
import "@fontsource-variable/onest";

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Mazmorras de Wakfu y sus guías" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta
      name="generator"
      content="Página con la lista de guías de mazmorras de Wakfu"
    />
    <title>{title}</title>
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  html {
    font-family: "Onest Variable", sans-serif;
  }

  body {
    background-color: #334;
    color: white;
  }

  @media (prefers-color-scheme: light) {
    body {
      background-color: #bcd;
      color: #334;
    }
  }
</style>
